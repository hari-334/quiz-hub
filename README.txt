QUIZ HUB:

Hello!!

To run Quiz Hub do the following steps:

1) Open terminal and run the command "npm install".
2) In the ".env" file change the database details to your local details.
3) Run the command "node setup.js".
4) Now, run the command "node server.js".
5) Open "localhost:3000" on your browser and enjoy Quiz Hub!!
    